import type { Exercise } from '../../types';
export const javafxExercises: Exercise[] = [
  { id: 'javafx-01', topicId: 'javafx', title: 'Stage und Scene einrichten', difficulty: 'leicht',
    description: 'Erstelle eine grundlegende JavaFX-Anwendung. Richte eine Stage (Fenster) mit einem Titel ein und erstelle eine Scene mit einem StackPane als Root-Element. Das Fenster soll 400x300 Pixel groß sein und den Titel "Meine erste App" haben. Ein Label mit dem Text "Hallo JavaFX!" soll zentriert angezeigt werden.',
    requirements: ['Application-Klasse erweitern und start() überschreiben', 'Stage-Titel auf "Meine erste App" setzen', 'StackPane als Root-Layout verwenden', 'Label mit Text "Hallo JavaFX!" hinzufügen', 'Scene mit 400x300 Pixel erstellen'],
    hints: ['extends Application und start(Stage stage) überschreiben', 'StackPane root = new StackPane()', 'root.getChildren().add(label)', 'stage.setScene(new Scene(root, 400, 300))'],
    starterCode: `import javafx.application.Application;\nimport javafx.scene.Scene;\nimport javafx.scene.control.Label;\nimport javafx.scene.layout.StackPane;\nimport javafx.stage.Stage;\n\npublic class Main extends Application {\n\n    @Override\n    public void start(Stage stage) {\n        // TODO: Label mit "Hallo JavaFX!" erstellen\n\n        // TODO: StackPane als Root-Layout erstellen\n\n        // TODO: Label zum Layout hinzufuegen\n\n        // TODO: Scene erstellen (400x300)\n\n        // TODO: Stage konfigurieren (Titel, Scene, show)\n    }\n\n    public static void main(String[] args) {\n        launch(args);\n    }\n}`,
    solutionCode: `import javafx.application.Application;\nimport javafx.scene.Scene;\nimport javafx.scene.control.Label;\nimport javafx.scene.layout.StackPane;\nimport javafx.stage.Stage;\n\npublic class Main extends Application {\n\n    @Override\n    public void start(Stage stage) {\n        Label label = new Label("Hallo JavaFX!");\n\n        StackPane root = new StackPane();\n        root.getChildren().add(label);\n\n        Scene scene = new Scene(root, 400, 300);\n\n        stage.setTitle("Meine erste App");\n        stage.setScene(scene);\n        stage.show();\n    }\n\n    public static void main(String[] args) {\n        launch(args);\n    }\n}`,
    expectedOutput: 'JavaFX Application erstellt',
    testCases: [{ expectedOutput: 'extends Application', description: 'Klasse erweitert Application' }, { expectedOutput: 'new Label', description: 'Label erstellt' }, { expectedOutput: 'new Scene', description: 'Scene erstellt' }, { expectedOutput: 'stage.show()', description: 'Stage wird angezeigt' }],
  },
  { id: 'javafx-02', topicId: 'javafx', title: 'Button mit EventHandler', difficulty: 'leicht',
    description: 'Erstelle eine JavaFX-Anwendung mit einem Button und einem Label. Wenn der Button geklickt wird, soll ein Zähler hochgezählt und im Label angezeigt werden. Verwende setOnAction() mit einem Lambda-Ausdruck als EventHandler.',
    requirements: ['Button mit Text "Klick mich!" erstellen', 'Label zur Anzeige des Zählers', 'setOnAction mit Lambda für Click-Event', 'Zähler bei jedem Klick erhöhen', 'VBox als Layout verwenden'],
    hints: ['button.setOnAction(e -> { ... }) für Event-Handling', 'Eine Instanzvariable oder AtomicInteger für den Zähler', 'label.setText("Klicks: " + zaehler) zum Aktualisieren', 'VBox ordnet Elemente vertikal an'],
    starterCode: `import javafx.application.Application;\nimport javafx.scene.Scene;\nimport javafx.scene.control.Button;\nimport javafx.scene.control.Label;\nimport javafx.scene.layout.VBox;\nimport javafx.stage.Stage;\nimport javafx.geometry.Pos;\n\npublic class Main extends Application {\n\n    private int zaehler = 0;\n\n    @Override\n    public void start(Stage stage) {\n        // TODO: Label fuer Zaehler-Anzeige erstellen\n\n        // TODO: Button "Klick mich!" erstellen\n\n        // TODO: EventHandler mit Lambda: Zaehler erhoehen und Label aktualisieren\n\n        // TODO: VBox-Layout erstellen und Elemente hinzufuegen\n\n        // TODO: Scene und Stage konfigurieren\n    }\n\n    public static void main(String[] args) {\n        launch(args);\n    }\n}`,
    solutionCode: `import javafx.application.Application;\nimport javafx.scene.Scene;\nimport javafx.scene.control.Button;\nimport javafx.scene.control.Label;\nimport javafx.scene.layout.VBox;\nimport javafx.stage.Stage;\nimport javafx.geometry.Pos;\n\npublic class Main extends Application {\n\n    private int zaehler = 0;\n\n    @Override\n    public void start(Stage stage) {\n        Label label = new Label("Klicks: 0");\n\n        Button button = new Button("Klick mich!");\n        button.setOnAction(e -> {\n            zaehler++;\n            label.setText("Klicks: " + zaehler);\n        });\n\n        VBox root = new VBox(10);\n        root.setAlignment(Pos.CENTER);\n        root.getChildren().addAll(label, button);\n\n        Scene scene = new Scene(root, 300, 200);\n        stage.setTitle("Klick-Zaehler");\n        stage.setScene(scene);\n        stage.show();\n    }\n\n    public static void main(String[] args) {\n        launch(args);\n    }\n}`,
    expectedOutput: 'JavaFX Button-Anwendung erstellt',
    testCases: [{ expectedOutput: 'new Button', description: 'Button erstellt' }, { expectedOutput: 'setOnAction', description: 'EventHandler registriert' }, { expectedOutput: 'setText', description: 'Label-Text wird aktualisiert' }, { expectedOutput: 'VBox', description: 'VBox-Layout verwendet' }],
  },
  { id: 'javafx-03', topicId: 'javafx', title: 'TextField und Label', difficulty: 'leicht',
    description: 'Erstelle ein Formular mit einem TextField zur Eingabe des Namens und einem Button "Begruessen". Beim Klick auf den Button soll in einem Label "Hallo, [Name]!" angezeigt werden. Wenn das Textfeld leer ist, soll "Bitte Namen eingeben!" erscheinen.',
    requirements: ['TextField für die Namenseingabe', 'Button "Begruessen" mit EventHandler', 'Label für die Ausgabe der Begrüßung', 'Validierung: leeres Feld abfangen', 'HBox oder VBox als Layout'],
    hints: ['textField.getText() gibt den eingegebenen Text zurück', 'text.isEmpty() prüft ob der String leer ist', 'label.setText("Hallo, " + name + "!") für die Begrüßung'],
    starterCode: `import javafx.application.Application;\nimport javafx.scene.Scene;\nimport javafx.scene.control.*;\nimport javafx.scene.layout.VBox;\nimport javafx.stage.Stage;\nimport javafx.geometry.Pos;\nimport javafx.geometry.Insets;\n\npublic class Main extends Application {\n\n    @Override\n    public void start(Stage stage) {\n        // TODO: TextField fuer Namenseingabe\n\n        // TODO: Button "Begruessen"\n\n        // TODO: Label fuer Ausgabe\n\n        // TODO: EventHandler: Name pruefen und begruessen\n\n        // TODO: Layout erstellen und Scene konfigurieren\n    }\n\n    public static void main(String[] args) {\n        launch(args);\n    }\n}`,
    solutionCode: `import javafx.application.Application;\nimport javafx.scene.Scene;\nimport javafx.scene.control.*;\nimport javafx.scene.layout.VBox;\nimport javafx.stage.Stage;\nimport javafx.geometry.Pos;\nimport javafx.geometry.Insets;\n\npublic class Main extends Application {\n\n    @Override\n    public void start(Stage stage) {\n        Label eingabeLabel = new Label("Name:");\n        TextField textField = new TextField();\n        textField.setPromptText("Name eingeben...");\n\n        Button button = new Button("Begruessen");\n        Label ausgabeLabel = new Label();\n\n        button.setOnAction(e -> {\n            String name = textField.getText().trim();\n            if (name.isEmpty()) {\n                ausgabeLabel.setText("Bitte Namen eingeben!");\n            } else {\n                ausgabeLabel.setText("Hallo, " + name + "!");\n            }\n        });\n\n        VBox root = new VBox(10);\n        root.setAlignment(Pos.CENTER);\n        root.setPadding(new Insets(20));\n        root.getChildren().addAll(eingabeLabel, textField, button, ausgabeLabel);\n\n        Scene scene = new Scene(root, 350, 250);\n        stage.setTitle("Begruessung");\n        stage.setScene(scene);\n        stage.show();\n    }\n\n    public static void main(String[] args) {\n        launch(args);\n    }\n}`,
    expectedOutput: 'JavaFX Formular erstellt',
    testCases: [{ expectedOutput: 'new TextField', description: 'TextField erstellt' }, { expectedOutput: 'getText()', description: 'Text aus Feld gelesen' }, { expectedOutput: 'isEmpty()', description: 'Leere Eingabe wird geprüft' }, { expectedOutput: 'Hallo, ', description: 'Begrüßung wird generiert' }],
  },
  { id: 'javafx-04', topicId: 'javafx', title: 'ListView verwenden', difficulty: 'mittel',
    description: 'Erstelle eine Anwendung mit einer ListView, einem TextField und zwei Buttons ("Hinzufuegen" und "Entfernen"). Der Benutzer kann über das TextField neue Einträge zur Liste hinzufügen. Mit dem Entfernen-Button wird der aktuell ausgewählte Eintrag gelöscht. Die ListView soll mit einer ObservableList verbunden sein.',
    requirements: ['ListView<String> mit ObservableList verbinden', 'TextField für neue Einträge', 'Button "Hinzufuegen" fügt Text zur Liste hinzu', 'Button "Entfernen" löscht ausgewähltes Element', 'Initiale Einträge: "Java", "Python", "JavaScript"'],
    hints: ['FXCollections.observableArrayList() für die ObservableList', 'listView.setItems(items) verbindet Liste mit View', 'listView.getSelectionModel().getSelectedItem() für Auswahl', 'items.add() und items.remove() aktualisieren automatisch die Anzeige'],
    starterCode: `import javafx.application.Application;\nimport javafx.collections.FXCollections;\nimport javafx.collections.ObservableList;\nimport javafx.scene.Scene;\nimport javafx.scene.control.*;\nimport javafx.scene.layout.*;\nimport javafx.stage.Stage;\nimport javafx.geometry.Insets;\n\npublic class Main extends Application {\n\n    @Override\n    public void start(Stage stage) {\n        // TODO: ObservableList mit initialen Eintraegen erstellen\n\n        // TODO: ListView erstellen und mit ObservableList verbinden\n\n        // TODO: TextField fuer neue Eintraege\n\n        // TODO: Button "Hinzufuegen" mit EventHandler\n\n        // TODO: Button "Entfernen" mit EventHandler\n\n        // TODO: Layout zusammenbauen\n    }\n\n    public static void main(String[] args) {\n        launch(args);\n    }\n}`,
    solutionCode: `import javafx.application.Application;\nimport javafx.collections.FXCollections;\nimport javafx.collections.ObservableList;\nimport javafx.scene.Scene;\nimport javafx.scene.control.*;\nimport javafx.scene.layout.*;\nimport javafx.stage.Stage;\nimport javafx.geometry.Insets;\n\npublic class Main extends Application {\n\n    @Override\n    public void start(Stage stage) {\n        ObservableList<String> items = FXCollections.observableArrayList(\n            "Java", "Python", "JavaScript"\n        );\n\n        ListView<String> listView = new ListView<>(items);\n        listView.setPrefHeight(200);\n\n        TextField textField = new TextField();\n        textField.setPromptText("Neuen Eintrag eingeben...");\n\n        Button btnHinzufuegen = new Button("Hinzufuegen");\n        btnHinzufuegen.setOnAction(e -> {\n            String text = textField.getText().trim();\n            if (!text.isEmpty()) {\n                items.add(text);\n                textField.clear();\n            }\n        });\n\n        Button btnEntfernen = new Button("Entfernen");\n        btnEntfernen.setOnAction(e -> {\n            String selected = listView.getSelectionModel().getSelectedItem();\n            if (selected != null) {\n                items.remove(selected);\n            }\n        });\n\n        HBox buttonBox = new HBox(10, btnHinzufuegen, btnEntfernen);\n\n        VBox root = new VBox(10);\n        root.setPadding(new Insets(15));\n        root.getChildren().addAll(listView, textField, buttonBox);\n\n        Scene scene = new Scene(root, 350, 400);\n        stage.setTitle("ListView Demo");\n        stage.setScene(scene);\n        stage.show();\n    }\n\n    public static void main(String[] args) {\n        launch(args);\n    }\n}`,
    expectedOutput: 'JavaFX ListView-Anwendung erstellt',
    testCases: [{ expectedOutput: 'ObservableList', description: 'ObservableList verwendet' }, { expectedOutput: 'new ListView', description: 'ListView erstellt' }, { expectedOutput: 'getSelectionModel', description: 'Auswahl-Modell verwendet' }, { expectedOutput: 'items.remove', description: 'Element-Entfernung implementiert' }],
  },
  { id: 'javafx-05', topicId: 'javafx', title: 'ComboBox und Auswahl', difficulty: 'mittel',
    description: 'Erstelle eine Anwendung mit einer ComboBox zur Auswahl einer Programmiersprache. Bei Änderung der Auswahl soll ein Label die Beschreibung der gewählten Sprache anzeigen. Verwende einen ChangeListener auf der selectedItem-Property der ComboBox.',
    requirements: ['ComboBox<String> mit Sprachen: Java, Python, JavaScript, C++, Rust', 'Label für die Beschreibung der gewählten Sprache', 'ChangeListener auf der Selection-Property', 'Jede Sprache hat eine eigene Beschreibung', 'setPromptText für den Anfangszustand'],
    hints: ['comboBox.setPromptText("Sprache waehlen...") für Platzhalter', 'comboBox.valueProperty().addListener((obs, alt, neu) -> { ... })', 'switch-Ausdruck für die Beschreibungen', 'getSelectionModel().selectFirst() für Vorauswahl möglich'],
    starterCode: `import javafx.application.Application;\nimport javafx.scene.Scene;\nimport javafx.scene.control.*;\nimport javafx.scene.layout.VBox;\nimport javafx.stage.Stage;\nimport javafx.geometry.Pos;\nimport javafx.geometry.Insets;\n\npublic class Main extends Application {\n\n    @Override\n    public void start(Stage stage) {\n        // TODO: ComboBox mit Sprachen erstellen\n\n        // TODO: Label fuer Beschreibung\n\n        // TODO: ChangeListener auf valueProperty\n\n        // TODO: Layout und Scene erstellen\n    }\n\n    // TODO: Methode getBeschreibung(String sprache)\n\n    public static void main(String[] args) {\n        launch(args);\n    }\n}`,
    solutionCode: `import javafx.application.Application;\nimport javafx.scene.Scene;\nimport javafx.scene.control.*;\nimport javafx.scene.layout.VBox;\nimport javafx.stage.Stage;\nimport javafx.geometry.Pos;\nimport javafx.geometry.Insets;\n\npublic class Main extends Application {\n\n    @Override\n    public void start(Stage stage) {\n        ComboBox<String> comboBox = new ComboBox<>();\n        comboBox.getItems().addAll("Java", "Python", "JavaScript", "C++", "Rust");\n        comboBox.setPromptText("Sprache waehlen...");\n\n        Label beschreibungLabel = new Label("Bitte eine Sprache waehlen.");\n        beschreibungLabel.setWrapText(true);\n\n        comboBox.valueProperty().addListener((obs, altWert, neuWert) -> {\n            if (neuWert != null) {\n                beschreibungLabel.setText(getBeschreibung(neuWert));\n            }\n        });\n\n        VBox root = new VBox(15);\n        root.setAlignment(Pos.CENTER);\n        root.setPadding(new Insets(20));\n        root.getChildren().addAll(new Label("Programmiersprache:"), comboBox, beschreibungLabel);\n\n        Scene scene = new Scene(root, 400, 250);\n        stage.setTitle("Sprach-Auswahl");\n        stage.setScene(scene);\n        stage.show();\n    }\n\n    private String getBeschreibung(String sprache) {\n        return switch (sprache) {\n            case "Java" -> "Java ist eine objektorientierte Sprache fuer plattformunabhaengige Anwendungen.";\n            case "Python" -> "Python ist eine interpretierte Sprache, beliebt fuer KI und Data Science.";\n            case "JavaScript" -> "JavaScript ist die Sprache des Webs fuer interaktive Webseiten.";\n            case "C++" -> "C++ ist eine performante Sprache fuer systemnahe Programmierung.";\n            case "Rust" -> "Rust ist eine sichere Systemsprache mit Speichersicherheit ohne Garbage Collector.";\n            default -> "Keine Beschreibung verfuegbar.";\n        };\n    }\n\n    public static void main(String[] args) {\n        launch(args);\n    }\n}`,
    expectedOutput: 'JavaFX ComboBox-Anwendung erstellt',
    testCases: [{ expectedOutput: 'new ComboBox', description: 'ComboBox erstellt' }, { expectedOutput: 'valueProperty().addListener', description: 'ChangeListener registriert' }, { expectedOutput: 'switch', description: 'Switch-Ausdruck für Beschreibungen' }, { expectedOutput: 'setPromptText', description: 'Platzhaltertext gesetzt' }],
  },
  { id: 'javafx-06', topicId: 'javafx', title: 'GridPane Layout', difficulty: 'mittel',
    description: 'Erstelle ein Registrierungsformular mit GridPane-Layout. Das Formular soll die Felder Vorname, Nachname, E-Mail und Passwort enthalten. Jedes Feld hat ein Label links und ein TextField/PasswordField rechts. Ein "Registrieren"-Button am Ende sammelt die Daten und zeigt sie in einem Label an.',
    requirements: ['GridPane als Layout mit Labels und TextFields', 'Felder: Vorname, Nachname, E-Mail, Passwort', 'PasswordField für das Passwort verwenden', 'Button "Registrieren" liest alle Felder aus', 'Ergebnis in einem Label unter dem Formular anzeigen'],
    hints: ['gridPane.add(node, spalte, zeile) zum Positionieren', 'gridPane.setHgap(10) und setVgap(10) für Abstände', 'PasswordField funktioniert wie TextField, zeigt aber Punkte', 'ColumnConstraints für gleichmäßige Spaltenbreiten'],
    starterCode: `import javafx.application.Application;\nimport javafx.scene.Scene;\nimport javafx.scene.control.*;\nimport javafx.scene.layout.*;\nimport javafx.stage.Stage;\nimport javafx.geometry.Insets;\nimport javafx.geometry.Pos;\n\npublic class Main extends Application {\n\n    @Override\n    public void start(Stage stage) {\n        // TODO: GridPane erstellen mit Abstaenden\n\n        // TODO: Labels und TextFields fuer Vorname, Nachname, E-Mail\n\n        // TODO: PasswordField fuer Passwort\n\n        // TODO: Button "Registrieren"\n\n        // TODO: Ergebnis-Label\n\n        // TODO: EventHandler: Felder auslesen und anzeigen\n\n        // TODO: Alle Elemente im GridPane positionieren\n    }\n\n    public static void main(String[] args) {\n        launch(args);\n    }\n}`,
    solutionCode: `import javafx.application.Application;\nimport javafx.scene.Scene;\nimport javafx.scene.control.*;\nimport javafx.scene.layout.*;\nimport javafx.stage.Stage;\nimport javafx.geometry.Insets;\nimport javafx.geometry.Pos;\n\npublic class Main extends Application {\n\n    @Override\n    public void start(Stage stage) {\n        GridPane grid = new GridPane();\n        grid.setHgap(10);\n        grid.setVgap(10);\n        grid.setPadding(new Insets(20));\n\n        Label lblVorname = new Label("Vorname:");\n        TextField tfVorname = new TextField();\n\n        Label lblNachname = new Label("Nachname:");\n        TextField tfNachname = new TextField();\n\n        Label lblEmail = new Label("E-Mail:");\n        TextField tfEmail = new TextField();\n\n        Label lblPasswort = new Label("Passwort:");\n        PasswordField pfPasswort = new PasswordField();\n\n        Button btnRegistrieren = new Button("Registrieren");\n        Label lblErgebnis = new Label();\n\n        btnRegistrieren.setOnAction(e -> {\n            String vorname = tfVorname.getText().trim();\n            String nachname = tfNachname.getText().trim();\n            String email = tfEmail.getText().trim();\n            String passwort = pfPasswort.getText();\n\n            if (vorname.isEmpty() || nachname.isEmpty() || email.isEmpty() || passwort.isEmpty()) {\n                lblErgebnis.setText("Bitte alle Felder ausfuellen!");\n            } else {\n                lblErgebnis.setText("Registriert: " + vorname + " " + nachname + " (" + email + ")");\n            }\n        });\n\n        grid.add(lblVorname, 0, 0);\n        grid.add(tfVorname, 1, 0);\n        grid.add(lblNachname, 0, 1);\n        grid.add(tfNachname, 1, 1);\n        grid.add(lblEmail, 0, 2);\n        grid.add(tfEmail, 1, 2);\n        grid.add(lblPasswort, 0, 3);\n        grid.add(pfPasswort, 1, 3);\n        grid.add(btnRegistrieren, 1, 4);\n        grid.add(lblErgebnis, 0, 5, 2, 1);\n\n        Scene scene = new Scene(grid, 400, 300);\n        stage.setTitle("Registrierung");\n        stage.setScene(scene);\n        stage.show();\n    }\n\n    public static void main(String[] args) {\n        launch(args);\n    }\n}`,
    expectedOutput: 'JavaFX GridPane-Formular erstellt',
    testCases: [{ expectedOutput: 'new GridPane', description: 'GridPane erstellt' }, { expectedOutput: 'grid.add', description: 'Elemente im Grid positioniert' }, { expectedOutput: 'new PasswordField', description: 'PasswordField verwendet' }, { expectedOutput: 'Registriert:', description: 'Registrierungsergebnis angezeigt' }],
  },
  { id: 'javafx-07', topicId: 'javafx', title: 'CSS-Styling in JavaFX', difficulty: 'mittel',
    description: 'Erstelle eine JavaFX-Anwendung und style sie mit CSS. Verwende sowohl Inline-Styles (setStyle) als auch eine externe CSS-Klasse über getStyleClass(). Die Anwendung soll einen Header mit großer Schrift, einen Button mit eigenem Hover-Effekt und ein gestyltes Label zeigen. Erstelle die CSS-Regeln als Inline-Styles oder über scene.getStylesheets().',
    requirements: ['Label als Header mit CSS-Styling (große Schrift, fett)', 'Button mit Hintergrundfarbe und abgerundeten Ecken', 'setStyle() für Inline-Styles verwenden', 'CSS-Klasse über getStyleClass().add() zuweisen', 'Mindestens 3 verschiedene CSS-Eigenschaften verwenden'],
    hints: ['node.setStyle("-fx-font-size: 24px; -fx-font-weight: bold;")', 'button.setStyle("-fx-background-color: #4CAF50; -fx-text-fill: white;")', '-fx-background-radius für abgerundete Ecken', 'scene.getStylesheets().add("style.css") für externe CSS-Datei'],
    starterCode: `import javafx.application.Application;\nimport javafx.scene.Scene;\nimport javafx.scene.control.*;\nimport javafx.scene.layout.VBox;\nimport javafx.stage.Stage;\nimport javafx.geometry.Pos;\nimport javafx.geometry.Insets;\n\npublic class Main extends Application {\n\n    @Override\n    public void start(Stage stage) {\n        // TODO: Header-Label mit CSS-Styling erstellen\n\n        // TODO: Gestylten Button erstellen\n\n        // TODO: Info-Label mit CSS-Styling\n\n        // TODO: VBox-Layout mit Hintergrundfarbe\n\n        // TODO: Scene erstellen\n    }\n\n    public static void main(String[] args) {\n        launch(args);\n    }\n}`,
    solutionCode: `import javafx.application.Application;\nimport javafx.scene.Scene;\nimport javafx.scene.control.*;\nimport javafx.scene.layout.VBox;\nimport javafx.stage.Stage;\nimport javafx.geometry.Pos;\nimport javafx.geometry.Insets;\n\npublic class Main extends Application {\n\n    @Override\n    public void start(Stage stage) {\n        Label header = new Label("Willkommen!");\n        header.setStyle("-fx-font-size: 28px; -fx-font-weight: bold; -fx-text-fill: #2c3e50;");\n\n        Button button = new Button("Aktion ausfuehren");\n        button.setStyle(\n            "-fx-background-color: #3498db; " +\n            "-fx-text-fill: white; " +\n            "-fx-font-size: 14px; " +\n            "-fx-padding: 10 20; " +\n            "-fx-background-radius: 5;"\n        );\n\n        button.setOnMouseEntered(e -> button.setStyle(\n            "-fx-background-color: #2980b9; " +\n            "-fx-text-fill: white; " +\n            "-fx-font-size: 14px; " +\n            "-fx-padding: 10 20; " +\n            "-fx-background-radius: 5; " +\n            "-fx-cursor: hand;"\n        ));\n        button.setOnMouseExited(e -> button.setStyle(\n            "-fx-background-color: #3498db; " +\n            "-fx-text-fill: white; " +\n            "-fx-font-size: 14px; " +\n            "-fx-padding: 10 20; " +\n            "-fx-background-radius: 5;"\n        ));\n\n        Label infoLabel = new Label("Diese App demonstriert CSS-Styling in JavaFX.");\n        infoLabel.setStyle("-fx-font-size: 13px; -fx-text-fill: #7f8c8d; -fx-font-style: italic;");\n\n        Label statusLabel = new Label("Bereit");\n        button.setOnAction(e -> {\n            statusLabel.setText("Aktion ausgefuehrt!");\n            statusLabel.setStyle("-fx-font-size: 14px; -fx-text-fill: #27ae60; -fx-font-weight: bold;");\n        });\n\n        VBox root = new VBox(20);\n        root.setAlignment(Pos.CENTER);\n        root.setPadding(new Insets(30));\n        root.setStyle("-fx-background-color: #ecf0f1;");\n        root.getChildren().addAll(header, infoLabel, button, statusLabel);\n\n        Scene scene = new Scene(root, 450, 300);\n        stage.setTitle("CSS-Styling Demo");\n        stage.setScene(scene);\n        stage.show();\n    }\n\n    public static void main(String[] args) {\n        launch(args);\n    }\n}`,
    expectedOutput: 'JavaFX CSS-Styling angewendet',
    testCases: [{ expectedOutput: 'setStyle', description: 'Inline-CSS-Styles verwendet' }, { expectedOutput: '-fx-font-size', description: 'Schriftgröße gesetzt' }, { expectedOutput: '-fx-background-color', description: 'Hintergrundfarbe gesetzt' }, { expectedOutput: '-fx-background-radius', description: 'Abgerundete Ecken verwendet' }],
  },
  { id: 'javafx-08', topicId: 'javafx', title: 'FXML-Struktur erstellen', difficulty: 'schwer',
    description: 'Erstelle eine JavaFX-Anwendung mit FXML. Schreibe sowohl die FXML-Datei (als String-Konstante) als auch die Controller-Klasse und die Application-Klasse. Die FXML beschreibt ein Formular mit einem Label, einem TextField, einem Button und einem Ausgabe-Label. Der Controller verarbeitet den Button-Klick.',
    requirements: ['FXML-Struktur als String-Konstante definieren (zum Verständnis)', 'Controller-Klasse mit @FXML-Annotationen', 'fx:controller Attribut in FXML', 'FXMLLoader in der Application-Klasse verwenden', 'onAction="#handleClick" für Button-Aktion in FXML'],
    hints: ['FXMLLoader loader = new FXMLLoader(getClass().getResource("view.fxml"))', '@FXML annotiert die Felder die in der FXML referenziert werden', 'fx:id="meinFeld" in FXML entspricht @FXML TextField meinFeld im Controller', 'onAction="#methodenName" ruft die Methode im Controller auf'],
    starterCode: `import javafx.application.Application;\nimport javafx.fxml.FXML;\nimport javafx.fxml.FXMLLoader;\nimport javafx.scene.Scene;\nimport javafx.scene.Parent;\nimport javafx.scene.control.*;\nimport javafx.stage.Stage;\n\n// === FXML-Struktur (normalerweise in view.fxml Datei) ===\n// TODO: FXML als Kommentar/String zeigen:\n// <?xml version="1.0" encoding="UTF-8"?>\n// <?import javafx.scene.layout.VBox?>\n// <?import javafx.scene.control.*?>\n// <VBox ...>\n//   <Label .../>\n//   <TextField fx:id="..." />\n//   <Button text="..." onAction="#..." />\n//   <Label fx:id="..." />\n// </VBox>\n\n// === Controller-Klasse ===\n// TODO: Controller mit @FXML-Feldern und Handler-Methode\n\n// === Application-Klasse ===\npublic class Main extends Application {\n\n    @Override\n    public void start(Stage stage) {\n        // TODO: FXML laden und Scene erstellen\n        // Hinweis: Fuer diese Uebung zeigen wir die Struktur\n        // und erstellen die UI programmatisch als Demonstration\n    }\n\n    public static void main(String[] args) {\n        launch(args);\n    }\n}`,
    solutionCode: `import javafx.application.Application;\nimport javafx.fxml.FXML;\nimport javafx.scene.Scene;\nimport javafx.scene.control.*;\nimport javafx.scene.layout.VBox;\nimport javafx.stage.Stage;\nimport javafx.geometry.Pos;\nimport javafx.geometry.Insets;\n\n/*\n * FXML-Datei (view.fxml) wuerde so aussehen:\n *\n * <?xml version="1.0" encoding="UTF-8"?>\n * <?import javafx.scene.layout.VBox?>\n * <?import javafx.scene.control.Label?>\n * <?import javafx.scene.control.TextField?>\n * <?import javafx.scene.control.Button?>\n *\n * <VBox alignment="CENTER" spacing="10" padding="20"\n *       xmlns:fx="http://javafx.com/fxml"\n *       fx:controller="MainController">\n *\n *     <Label text="Geben Sie Ihren Namen ein:"/>\n *     <TextField fx:id="nameField" promptText="Name..."/>\n *     <Button text="Begruessen" onAction="#handleBegruessen"/>\n *     <Label fx:id="ausgabeLabel" text=""/>\n * </VBox>\n */\n\n// Controller-Klasse (normalerweise in MainController.java)\nclass MainController {\n    @FXML\n    private TextField nameField;\n\n    @FXML\n    private Label ausgabeLabel;\n\n    @FXML\n    private void handleBegruessen() {\n        String name = nameField.getText().trim();\n        if (name.isEmpty()) {\n            ausgabeLabel.setText("Bitte einen Namen eingeben!");\n        } else {\n            ausgabeLabel.setText("Hallo, " + name + "! Willkommen!");\n        }\n    }\n}\n\n// Application-Klasse - hier programmatisch statt FXML (lauffaehige Demo)\npublic class Main extends Application {\n\n    @Override\n    public void start(Stage stage) {\n        // In einer echten FXML-Anwendung:\n        // Parent root = FXMLLoader.load(getClass().getResource("view.fxml"));\n\n        // Programmatische Erstellung (gleiche Struktur wie FXML):\n        Label headerLabel = new Label("Geben Sie Ihren Namen ein:");\n        TextField nameField = new TextField();\n        nameField.setPromptText("Name...");\n        Button button = new Button("Begruessen");\n        Label ausgabeLabel = new Label("");\n\n        // EventHandler (entspricht onAction="#handleBegruessen")\n        button.setOnAction(e -> {\n            String name = nameField.getText().trim();\n            if (name.isEmpty()) {\n                ausgabeLabel.setText("Bitte einen Namen eingeben!");\n            } else {\n                ausgabeLabel.setText("Hallo, " + name + "! Willkommen!");\n            }\n        });\n\n        VBox root = new VBox(10);\n        root.setAlignment(Pos.CENTER);\n        root.setPadding(new Insets(20));\n        root.getChildren().addAll(headerLabel, nameField, button, ausgabeLabel);\n\n        Scene scene = new Scene(root, 400, 250);\n        stage.setTitle("FXML-Demo");\n        stage.setScene(scene);\n        stage.show();\n\n        System.out.println("FXML-Struktur demonstriert");\n        System.out.println("Controller: MainController mit @FXML Annotationen");\n        System.out.println("Felder: nameField (TextField), ausgabeLabel (Label)");\n        System.out.println("Aktion: onAction=handleBegruessen");\n    }\n\n    public static void main(String[] args) {\n        launch(args);\n    }\n}`,
    expectedOutput: 'JavaFX FXML-Struktur demonstriert',
    testCases: [{ expectedOutput: '@FXML', description: 'FXML-Annotation verwendet' }, { expectedOutput: 'fx:controller', description: 'Controller in FXML referenziert' }, { expectedOutput: 'fx:id', description: 'FXML-IDs vergeben' }, { expectedOutput: 'onAction', description: 'Event-Handler in FXML definiert' }, { expectedOutput: 'FXMLLoader', description: 'FXMLLoader erwähnt' }],
  },
];
