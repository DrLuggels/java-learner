import type { Exercise } from '../../types';
export const baeumeExercises: Exercise[] = [
  { id: 'baeume-01', topicId: 'baeume', title: 'Binärer Suchbaum', difficulty: 'schwer',
    description: 'Implementiere einen einfachen binären Suchbaum (BST) mit den Operationen Einfügen (insert) und Inorder-Traversierung. Ein binärer Suchbaum ordnet kleinere Werte links und größere Werte rechts ein. Die Inorder-Traversierung gibt die Werte in aufsteigender Reihenfolge aus.',
    requirements: ['Klasse Knoten mit wert, links und rechts', 'Klasse BinaerBaum mit Wurzel-Knoten', 'Methode insert(int wert) zum Einfügen', 'Methode inorder() für Inorder-Traversierung (links, wurzel, rechts)', 'Ausgabe der Werte in sortierter Reihenfolge'],
    hints: ['Ein Knoten hat einen Wert und Referenzen auf linkes und rechtes Kind', 'Beim Einfügen: kleiner -> links, größer -> rechts', 'Inorder: erst links rekursiv, dann Wert ausgeben, dann rechts rekursiv', 'Rekursion ist der Schlüssel für beide Operationen'],
    starterCode: `public class Main {\n    // TODO: Klasse Knoten mit wert, links, rechts\n\n    // TODO: Klasse BinaerBaum mit wurzel\n    // TODO: Methode insert(int wert)\n    // TODO: Methode inorder()\n\n    public static void main(String[] args) {\n        // TODO: Baum erstellen und Werte einfuegen: 50, 30, 70, 20, 40, 60, 80\n        // TODO: Inorder-Traversierung ausgeben\n    }\n}`,
    solutionCode: `public class Main {\n    static class Knoten {\n        int wert;\n        Knoten links, rechts;\n\n        Knoten(int wert) {\n            this.wert = wert;\n            this.links = null;\n            this.rechts = null;\n        }\n    }\n\n    static class BinaerBaum {\n        Knoten wurzel;\n\n        BinaerBaum() {\n            this.wurzel = null;\n        }\n\n        void insert(int wert) {\n            wurzel = insertRek(wurzel, wert);\n        }\n\n        private Knoten insertRek(Knoten knoten, int wert) {\n            if (knoten == null) {\n                return new Knoten(wert);\n            }\n            if (wert < knoten.wert) {\n                knoten.links = insertRek(knoten.links, wert);\n            } else if (wert > knoten.wert) {\n                knoten.rechts = insertRek(knoten.rechts, wert);\n            }\n            return knoten;\n        }\n\n        void inorder() {\n            inorderRek(wurzel);\n            System.out.println();\n        }\n\n        private void inorderRek(Knoten knoten) {\n            if (knoten != null) {\n                inorderRek(knoten.links);\n                System.out.print(knoten.wert + " ");\n                inorderRek(knoten.rechts);\n            }\n        }\n    }\n\n    public static void main(String[] args) {\n        BinaerBaum baum = new BinaerBaum();\n        int[] werte = {50, 30, 70, 20, 40, 60, 80};\n\n        System.out.println("Einfuegen der Werte:");\n        for (int w : werte) {\n            baum.insert(w);\n            System.out.print(w + " ");\n        }\n        System.out.println();\n\n        System.out.println("Inorder-Traversierung (sortiert):");\n        baum.inorder();\n    }\n}`,
    expectedOutput: 'Einfuegen der Werte:\n50 30 70 20 40 60 80 \nInorder-Traversierung (sortiert):\n20 30 40 50 60 70 80',
    testCases: [{ expectedOutput: '20 30 40 50 60 70 80', description: 'Inorder-Traversierung gibt sortierte Reihenfolge aus' }, { expectedOutput: '50 30 70 20 40 60 80', description: 'Alle Werte wurden eingefügt' }],
  },
];
