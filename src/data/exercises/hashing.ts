import type { Exercise } from '../../types';
export const hashingExercises: Exercise[] = [
  { id: 'hashing-01', topicId: 'hashing', title: 'hashCode und equals überschreiben', difficulty: 'mittel',
    description: 'Erstelle eine Klasse Person mit den Attributen name (String) und alter (int). Überschreibe die Methoden hashCode() und equals(), sodass zwei Personen als gleich gelten, wenn Name und Alter übereinstimmen. Teste dies, indem du zwei identische Personen erstellst und sie in einem HashSet speicherst.',
    requirements: ['Klasse Person mit name und alter', 'equals() korrekt überschreiben mit instanceof-Prüfung', 'hashCode() mit Objects.hash() implementieren', 'HashSet verwenden um Duplikaterkennung zu zeigen'],
    hints: ['Verwende @Override vor equals und hashCode', 'In equals: erst prüfen ob obj instanceof Person', 'Objects.hash(name, alter) für hashCode', 'Ein HashSet speichert keine Duplikate wenn equals/hashCode korrekt sind'],
    starterCode: `import java.util.*;\n\npublic class Main {\n    // TODO: Klasse Person mit name und alter\n    // TODO: equals() ueberschreiben\n    // TODO: hashCode() ueberschreiben\n    // TODO: toString() ueberschreiben\n\n    public static void main(String[] args) {\n        // TODO: Zwei identische Personen erstellen\n        // TODO: equals testen\n        // TODO: In HashSet speichern und Groesse pruefen\n    }\n}`,
    solutionCode: `import java.util.*;\n\npublic class Main {\n    static class Person {\n        String name;\n        int alter;\n\n        Person(String name, int alter) {\n            this.name = name;\n            this.alter = alter;\n        }\n\n        @Override\n        public boolean equals(Object obj) {\n            if (this == obj) return true;\n            if (!(obj instanceof Person other)) return false;\n            return this.alter == other.alter && Objects.equals(this.name, other.name);\n        }\n\n        @Override\n        public int hashCode() {\n            return Objects.hash(name, alter);\n        }\n\n        @Override\n        public String toString() {\n            return name + " (" + alter + ")";\n        }\n    }\n\n    public static void main(String[] args) {\n        Person p1 = new Person("Max", 25);\n        Person p2 = new Person("Max", 25);\n        Person p3 = new Person("Anna", 30);\n\n        System.out.println("p1.equals(p2): " + p1.equals(p2));\n        System.out.println("p1.equals(p3): " + p1.equals(p3));\n        System.out.println("p1.hashCode == p2.hashCode: " + (p1.hashCode() == p2.hashCode()));\n\n        Set<Person> set = new HashSet<>();\n        set.add(p1);\n        set.add(p2);\n        set.add(p3);\n        System.out.println("Set-Groesse: " + set.size());\n        System.out.println("Set: " + set);\n    }\n}`,
    expectedOutput: 'p1.equals(p2): true\np1.equals(p3): false\np1.hashCode == p2.hashCode: true\nSet-Groesse: 2',
    testCases: [{ expectedOutput: 'p1.equals(p2): true', description: 'Gleiche Personen sind equals' }, { expectedOutput: 'Set-Groesse: 2', description: 'HashSet erkennt Duplikate korrekt' }],
  },
  { id: 'hashing-02', topicId: 'hashing', title: 'Hash-Kollisionen demonstrieren', difficulty: 'schwer',
    description: 'Demonstriere Hash-Kollisionen mit einer eigenen Klasse, deren hashCode() absichtlich nur wenige verschiedene Werte liefert. Erstelle eine Klasse Schluessel, deren hashCode() immer den Wert (id % 3) zurückgibt. Füge mehrere Einträge in eine HashMap ein und zeige, dass trotz gleicher Hash-Werte unterschiedliche Schlüssel korrekt gespeichert werden.',
    requirements: ['Klasse Schluessel mit id und name', 'hashCode() gibt (id % 3) zurück um Kollisionen zu erzwingen', 'equals() korrekt implementieren', 'Mehrere Einträge in HashMap einfügen', 'Zeigen dass alle Einträge trotz Kollisionen korrekt abrufbar sind'],
    hints: ['Hash-Kollision bedeutet: verschiedene Objekte haben gleichen hashCode', 'HashMap löst Kollisionen intern über verkettete Listen oder Bäume', 'Trotz Kollision müssen equals und get korrekt funktionieren'],
    starterCode: `import java.util.*;\n\npublic class Main {\n    // TODO: Klasse Schluessel mit id und name\n    // TODO: hashCode() so implementieren, dass Kollisionen entstehen\n    // TODO: equals() korrekt implementieren\n\n    public static void main(String[] args) {\n        // TODO: HashMap mit Schluessel als Key erstellen\n        // TODO: Mehrere Eintraege einfuegen\n        // TODO: Hash-Werte und Abruf demonstrieren\n    }\n}`,
    solutionCode: `import java.util.*;\n\npublic class Main {\n    static class Schluessel {\n        int id;\n        String name;\n\n        Schluessel(int id, String name) {\n            this.id = id;\n            this.name = name;\n        }\n\n        @Override\n        public int hashCode() {\n            return id % 3; // Erzwingt Kollisionen\n        }\n\n        @Override\n        public boolean equals(Object obj) {\n            if (this == obj) return true;\n            if (!(obj instanceof Schluessel other)) return false;\n            return this.id == other.id && Objects.equals(this.name, other.name);\n        }\n\n        @Override\n        public String toString() {\n            return "Schluessel(" + id + ", " + name + ")";\n        }\n    }\n\n    public static void main(String[] args) {\n        Map<Schluessel, String> map = new HashMap<>();\n\n        Schluessel s1 = new Schluessel(1, "A");\n        Schluessel s2 = new Schluessel(4, "B");\n        Schluessel s3 = new Schluessel(7, "C");\n        Schluessel s4 = new Schluessel(2, "D");\n\n        map.put(s1, "Wert1");\n        map.put(s2, "Wert2");\n        map.put(s3, "Wert3");\n        map.put(s4, "Wert4");\n\n        System.out.println("Hash von s1 (id=1): " + s1.hashCode());\n        System.out.println("Hash von s2 (id=4): " + s2.hashCode());\n        System.out.println("Hash von s3 (id=7): " + s3.hashCode());\n        System.out.println("Kollision s1/s2/s3: " + (s1.hashCode() == s2.hashCode() && s2.hashCode() == s3.hashCode()));\n\n        System.out.println("Map-Groesse: " + map.size());\n        System.out.println("s1 -> " + map.get(s1));\n        System.out.println("s2 -> " + map.get(s2));\n        System.out.println("s3 -> " + map.get(s3));\n        System.out.println("s4 -> " + map.get(s4));\n    }\n}`,
    expectedOutput: 'Hash von s1 (id=1): 1\nHash von s2 (id=4): 1\nHash von s3 (id=7): 1\nKollision s1/s2/s3: true\nMap-Groesse: 4\ns1 -> Wert1\ns2 -> Wert2\ns3 -> Wert3\ns4 -> Wert4',
    testCases: [{ expectedOutput: 'Kollision s1/s2/s3: true', description: 'Hash-Kollision wird erzeugt' }, { expectedOutput: 'Map-Groesse: 4', description: 'Alle Einträge trotz Kollision gespeichert' }, { expectedOutput: 's2 -> Wert2', description: 'Werte trotz Kollision korrekt abrufbar' }],
  },
];
