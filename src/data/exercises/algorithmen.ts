import type { Exercise } from '../../types';
export const algorithmenExercises: Exercise[] = [
  { id: 'algorithmen-01', topicId: 'algorithmen', title: 'Bubble Sort', difficulty: 'mittel',
    description: 'Implementiere den Bubble-Sort-Algorithmus. Bubble Sort vergleicht wiederholt benachbarte Elemente und tauscht sie, wenn sie in der falschen Reihenfolge stehen. Der Algorithmus durchläuft das Array mehrfach, bis keine Vertauschungen mehr nötig sind.',
    requirements: ['Methode bubbleSort(int[] arr) implementieren', 'Benachbarte Elemente vergleichen und ggf. tauschen', 'Optimierung: Abbruch wenn keine Vertauschung in einem Durchlauf', 'Array vor und nach dem Sortieren ausgeben'],
    hints: ['Äußere Schleife: Anzahl der Durchläufe (arr.length - 1)', 'Innere Schleife: Vergleich benachbarter Elemente', 'Tausch mit Hilfsvariable: int temp = arr[j]; arr[j] = arr[j+1]; arr[j+1] = temp;', 'boolean-Flag für Optimierung: wenn kein Tausch -> fertig'],
    starterCode: `import java.util.Arrays;\n\npublic class Main {\n    // TODO: Methode bubbleSort(int[] arr)\n\n    public static void main(String[] args) {\n        int[] zahlen = {64, 34, 25, 12, 22, 11, 90};\n        System.out.println("Vorher:  " + Arrays.toString(zahlen));\n        // TODO: bubbleSort aufrufen\n        System.out.println("Nachher: " + Arrays.toString(zahlen));\n    }\n}`,
    solutionCode: `import java.util.Arrays;\n\npublic class Main {\n    static void bubbleSort(int[] arr) {\n        int n = arr.length;\n        for (int i = 0; i < n - 1; i++) {\n            boolean getauscht = false;\n            for (int j = 0; j < n - 1 - i; j++) {\n                if (arr[j] > arr[j + 1]) {\n                    int temp = arr[j];\n                    arr[j] = arr[j + 1];\n                    arr[j + 1] = temp;\n                    getauscht = true;\n                }\n            }\n            if (!getauscht) break;\n        }\n    }\n\n    public static void main(String[] args) {\n        int[] zahlen = {64, 34, 25, 12, 22, 11, 90};\n        System.out.println("Vorher:  " + Arrays.toString(zahlen));\n        bubbleSort(zahlen);\n        System.out.println("Nachher: " + Arrays.toString(zahlen));\n    }\n}`,
    expectedOutput: 'Vorher:  [64, 34, 25, 12, 22, 11, 90]\nNachher: [11, 12, 22, 25, 34, 64, 90]',
    testCases: [{ expectedOutput: '[11, 12, 22, 25, 34, 64, 90]', description: 'Array ist korrekt sortiert' }, { expectedOutput: 'Vorher:  [64, 34, 25, 12, 22, 11, 90]', description: 'Ursprüngliches Array korrekt ausgegeben' }],
  },
  { id: 'algorithmen-02', topicId: 'algorithmen', title: 'Binäre Suche', difficulty: 'mittel',
    description: 'Implementiere den Algorithmus der binären Suche auf einem sortierten Array. Die binäre Suche halbiert den Suchbereich in jedem Schritt, indem sie das mittlere Element mit dem Suchwert vergleicht. Dadurch ist sie mit O(log n) deutlich schneller als die lineare Suche O(n).',
    requirements: ['Methode binaereSuche(int[] arr, int ziel) implementieren', 'Rückgabe: Index des gefundenen Elements oder -1', 'links, rechts und mitte Variablen verwenden', 'Suchschritte ausgeben um den Algorithmus nachzuvollziehen'],
    hints: ['Start: links = 0, rechts = arr.length - 1', 'Mitte berechnen: mitte = links + (rechts - links) / 2', 'Wenn arr[mitte] == ziel -> gefunden', 'Wenn arr[mitte] < ziel -> links = mitte + 1, sonst rechts = mitte - 1'],
    starterCode: `import java.util.Arrays;\n\npublic class Main {\n    // TODO: Methode binaereSuche(int[] arr, int ziel)\n    // Soll den Index zurueckgeben oder -1 wenn nicht gefunden\n\n    public static void main(String[] args) {\n        int[] sortiert = {2, 5, 8, 12, 16, 23, 38, 56, 72, 91};\n        System.out.println("Array: " + Arrays.toString(sortiert));\n\n        // TODO: Nach 23 suchen\n        // TODO: Nach 99 suchen (nicht vorhanden)\n    }\n}`,
    solutionCode: `import java.util.Arrays;\n\npublic class Main {\n    static int binaereSuche(int[] arr, int ziel) {\n        int links = 0;\n        int rechts = arr.length - 1;\n        int schritt = 1;\n\n        while (links <= rechts) {\n            int mitte = links + (rechts - links) / 2;\n            System.out.println("  Schritt " + schritt + ": Bereich [" + links + ".." + rechts + "], Mitte=" + mitte + " (Wert=" + arr[mitte] + ")");\n\n            if (arr[mitte] == ziel) {\n                return mitte;\n            } else if (arr[mitte] < ziel) {\n                links = mitte + 1;\n            } else {\n                rechts = mitte - 1;\n            }\n            schritt++;\n        }\n        return -1;\n    }\n\n    public static void main(String[] args) {\n        int[] sortiert = {2, 5, 8, 12, 16, 23, 38, 56, 72, 91};\n        System.out.println("Array: " + Arrays.toString(sortiert));\n\n        System.out.println("Suche nach 23:");\n        int idx1 = binaereSuche(sortiert, 23);\n        System.out.println("Ergebnis: Index " + idx1);\n\n        System.out.println("Suche nach 99:");\n        int idx2 = binaereSuche(sortiert, 99);\n        System.out.println("Ergebnis: Index " + idx2 + " (nicht gefunden)");\n    }\n}`,
    expectedOutput: 'Array: [2, 5, 8, 12, 16, 23, 38, 56, 72, 91]\nSuche nach 23:\n  Schritt 1: Bereich [0..9], Mitte=4 (Wert=16)\n  Schritt 2: Bereich [5..9], Mitte=7 (Wert=56)\n  Schritt 3: Bereich [5..6], Mitte=5 (Wert=23)\nErgebnis: Index 5\nSuche nach 99:\n  Schritt 1: Bereich [0..9], Mitte=4 (Wert=16)\n  Schritt 2: Bereich [5..9], Mitte=7 (Wert=56)\n  Schritt 3: Bereich [8..9], Mitte=8 (Wert=72)\n  Schritt 4: Bereich [9..9], Mitte=9 (Wert=91)\nErgebnis: Index -1 (nicht gefunden)',
    testCases: [{ expectedOutput: 'Ergebnis: Index 5', description: '23 wird an Index 5 gefunden' }, { expectedOutput: 'Ergebnis: Index -1 (nicht gefunden)', description: '99 wird nicht gefunden' }],
  },
];
